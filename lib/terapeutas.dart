
import 'package:flutter/material.dart';

class TerapeutasScreen extends StatelessWidget {
  const TerapeutasScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Terapeutas Screen'),
      ),
    );
  }
}
