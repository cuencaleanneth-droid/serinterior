# 🚀 Guía Completa: Inicio de Sesión con Google + Firebase en Flutter

> Configuración actualizada para `google_sign_in: ^6.2.0` y `firebase_auth` en Flutter 3.24+  
> Compatible con Android, iOS y Web.

---

## 🧱 1. Crear o abrir tu proyecto Flutter

Si no tienes uno:

```bash
flutter create myapp
cd myapp
